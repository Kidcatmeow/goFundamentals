package main

func main() {
	// lib.Variable()
	// lib.Different_print()
	// lib.UndeclaredVariable()
	// lib.BasicTypes()
	// lib.Const()
	// lib.If()
	// lib.Switch()

	//Functions
	// lib.Info("Watsamon", "Yoyo", 22)
	// day_from_Today := lib.Today()
	// fmt.Println(day_from_Today)

	// a, b := lib.Swap("one", "two")
	// fmt.Println(a, b)

	// c := lib.Plus(2, 5)
	// fmt.Println(c)
	// fmt.Printf("type: %T\n", lib.Plus)

	// //We basically send Plus and Monus function into Cal
	// lib.Cal(lib.Plus)
	// lib.Cal(lib.Minus)

	// //Arrays
	// fmt.Println(lib.LangsValues)
	// //to print info of the array, use '#'
	// fmt.Printf("details of the array: %#v\n", lib.LangsValues)
	// //to print values within the array
	// n := lib.LangsValues[1]
	// fmt.Println(n)
	// //to change the value within the array
	// lib.LangsValues[1] = "Kotlin"
	// b := lib.LangsValues[1]
	// fmt.Println(b)
	// //pass one array as a parameter for another function
	// //but the length and type of the array of both function must be the same
	// lib.ShowAll(lib.LangsValues)

	// lib.SLice()
}
